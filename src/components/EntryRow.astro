---
interface Props {
  href: string;
  number: string;
  title: string;
  description?: string;
  date: Date;
}

const { href, number, title, description, date } = Astro.props;

const formatted = date.toLocaleDateString("en-GB", {
  day: "2-digit",
  month: "short",
  year: "numeric",
});
---

<a href={href} class="row">
  <span class="number label label--accent">{number}</span>
  <span class="body">
    <span class="title">{title}</span>
    {description && <span class="description">{description}</span>}
  </span>
  <span class="date label">{formatted}</span>
</a>

<style>
  .row {
    display: grid;
    grid-template-columns: 3.5rem 1fr auto;
    gap: 1.25rem;
    align-items: baseline;
    padding: 1.25rem 0;
    border-bottom: 1px solid var(--rule);
    text-decoration: none;
  }

  .row:hover .title {
    color: var(--accent);
  }

  .title {
    display: block;
    font-weight: 600;
    letter-spacing: 0.02em;
  }

  .description {
    display: block;
    margin-top: 0.35rem;
    font-size: 0.8125rem;
    color: var(--ink-soft);
  }

  .date {
    white-space: nowrap;
  }

  @media (max-width: 540px) {
    .row {
      grid-template-columns: 3.5rem 1fr;
    }

    .date {
      grid-column: 2;
    }
  }
</style>
